# Pinned third-party image tags for reproducible deploys.
# Bump deliberately after testing; CI scans built images on every push.
NEO4J_IMAGE=neo4j:5.26-community
POSTGRES_IMAGE=postgres:16-alpine
NETBOX_IMAGE=netboxcommunity/netbox:v4.1-3.0.2
REDIS_IMAGE=redis:7-alpine
API_BASE_IMAGE=python:3.13-slim
WEB_BASE_IMAGE=node:22-alpine
